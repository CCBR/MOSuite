# Filtered counts for the NIDAP test dataset. The result of running `filter_counts()` on `nidap_clean_raw_counts`.

Filtered counts for the NIDAP test dataset. The result of running
[`filter_counts()`](https://ccbr.github.io/MOSuite/reference/filter_counts.md)
on `nidap_clean_raw_counts`.

## Usage

``` r
nidap_filtered_counts
```

## Format

An object of class `spec_tbl_df` (inherits from `tbl_df`, `tbl`,
`data.frame`) with 7943 rows and 10 columns.
