# Summarized differential expression analysis for input to venn diagram

Summarized differential expression analysis for input to venn diagram

## Usage

``` r
nidap_volcano_summary_dat
```

## Format

An object of class `spec_tbl_df` (inherits from `tbl_df`, `tbl`,
`data.frame`) with 4929 rows and 7 columns.
