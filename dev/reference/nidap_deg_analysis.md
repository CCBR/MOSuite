# Differential gene expression analysis for the NIDAP test dataset.

Differential gene expression analysis for the NIDAP test dataset.

## Usage

``` r
nidap_deg_analysis
```

## Format

An object of class `spec_tbl_df` (inherits from `tbl_df`, `tbl`,
`data.frame`) with 7943 rows and 25 columns.
